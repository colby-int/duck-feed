# Optional production overrides for deployments where the SPA, API, and stream
# do not share one origin. Leave client/.env.production absent when Nginx serves
# everything from the same host.
VITE_API_URL=https://radio.example.com
VITE_STREAM_URL=https://radio.example.com/stream
